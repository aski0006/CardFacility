namespace _Core._interface {
    public interface IGameBase {
         // 暂停函数
        void Pause();
         // 恢复函数
        void Resume();
         // 定义一个名为LogDebugInfo的函数，用于记录调试信息
        void LogDebugInfo();
    }
}
