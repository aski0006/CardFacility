namespace _Enums{
    public enum ResourceType {
        ConceptShard, // 概念碎片(CS)
        PureLogic, // 纯化逻辑(PL)
        RealityAnchor, // 现实稳定锚(RA)
        EntropyPollution, // 熵污染(EP)
        CognitiveLoad // 认知负荷(CL)
    }
}
